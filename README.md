# resume-builder

A small Node.js tool that generates a clean, ATS-friendly `.docx` resume
from a JSON config — no Word template wrangling required.

## Usage

```bash
npm install
node src/build.js example/resume.config.json output.docx
